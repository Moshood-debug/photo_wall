
/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["10.120.69.203", "localhost:3000", "*.local"],
};

export default nextConfig;
